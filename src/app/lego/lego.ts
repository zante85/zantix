import { GenericElement } from './genericelement';

export class Lego {
    id: string;
    title: string;
    description: string;
    background-image: string;
    icon: string;
    text: string;
    elements: GenericElement[];
}
