import { GenericElement } from './genericelement';

export class Lego {
    id: string;
    title: string;
    description: string;
    backgroundimage: string;
    icon: string;
    text: string;
    elements: GenericElement[];
}
